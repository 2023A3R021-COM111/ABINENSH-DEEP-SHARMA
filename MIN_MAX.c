#include<stdio.h>
#include<limits.h>
int main()
{
    //checking the maximum and minimum value in a 32 bit compiler
    printf("minimum value of int: %d\n", INT_MIN);
        printf("maximum value of int: %d\n", INT_MAX);
 return 0;
}